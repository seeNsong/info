
clone 후 disconnect : 
  git remote -v
  git remote rm origin(target link)

Remove untracked files&directory from the working tree
  git clean -fd

delete target folder :
  git rm -r <target folder>

push local rep to remote rep : 
  remote add origin [addr.git]
  push origin [branch]
